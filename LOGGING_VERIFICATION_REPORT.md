# Отчёт о проверке логирования в приложении

Дата проверки: 2024
Ветка: `verify-logging-enabled-on-real-device`

## ✅ Результаты проверки кода

### 1. Логирование включено по-умолчанию

**Проверено:** `core/settings/src/main/kotlin/com/kotopogoda/uploader/core/settings/SettingsRepositoryImpl.kt`

```kotlin
// Строка 50
appLogging = preferences[APP_LOGGING_KEY] ?: true,

// Строка 51
httpLogging = preferences[HTTP_LOGGING_KEY] ?: true,
```

✅ **Подтверждено:** Оба вида логирования (`appLogging` и `httpLogging`) включены по-умолчанию при первом запуске приложения.

---

### 2. Инициализация логирования в Application

**Проверено:** `app/src/main/java/com/kotopogoda/uploader/KotopogodaUploaderApp.kt`

```kotlin
// Строка 112 - onCreate()
appLogger.setEnabled(true)

// Строка 168 - реактивное обновление
settingsRepository.flow.collect { settings ->
    appLogger.setEnabled(settings.appLogging)
    httpLoggingController.setEnabled(settings.httpLogging)
    // ...
}
```

✅ **Подтверждено:** Логирование инициализируется немедленно при старте приложения и реактивно обновляется при изменении настроек.

---

### 3. Конфигурация файлового логирования

**Проверено:** `core/logging/src/main/kotlin/com/kotopogoda/uploader/core/logging/`

| Параметр | Значение |
|----------|----------|
| Директория | `/data/data/com.kotopogoda.uploader/files/logs/` |
| Базовое имя файла | `app` (app.log, app.1.log, ..., app.4.log) |
| Максимум файлов | 5 |
| Максимальный размер файла | 1 MB |
| Формат записи | `[YYYY-MM-DD HH:MM:SS.mmm][LEVEL] TAG: message` |
| Стеки исключений | Логируются полностью через `Log.getStackTraceString(t)` |

✅ **Подтверждено:** Файловое логирование настроено с ротацией, ограничением размера и структурированным форматом.

---

### 4. Логируемые события

**Проверено:** Анализ использования `Timber` и `UploadLog` в кодовой базе

Приложение логирует:

- ✅ **Старт приложения:** `APP/START` / `application_create`
- ✅ **Изменения настроек:** `CFG/STATE` / `settings` (baseUrl, appLogging, httpLogging, persistentQueueNotification)
- ✅ **Разрешения уведомлений:** `PERM/STATE` / `notifications`
- ✅ **WorkManager события:** `WORK/Factory` (инициализация, дубликаты)
- ✅ **Фатальные крэши:** `FATAL/CRASH` / `uncaught_exception` (thread, coroutine)
- ✅ **Загрузка ML моделей:** Ошибки загрузки нативных библиотек и EnhancerModelProbe
- ✅ **Enhance операции:** Verbose логирование в DEBUG билдах через `EnhanceFileLogger`

✅ **Подтверждено:** Логируются все ключевые события жизненного цикла приложения.

---

### 5. Доступ к логам через код

**Проверено:** `core/logging/src/main/kotlin/com/kotopogoda/uploader/core/logging/LogManager.kt`

API для работы с логами:

```kotlin
suspend fun listLogFiles(): List<File>
suspend fun writeLogsArchive(outputStream: OutputStream): Boolean
fun logsDirectory(): File
fun logsDirectoryPath(): String
```

✅ **Подтверждено:** Есть программный API для экспорта логов в ZIP архив с добавлением `diagnostics.json`.

---

## ⚠️ Обнаруженные проблемы

### Проблема 1: Отсутствие DebugTree для logcat

**Описание:** В коде не обнаружена установка `Timber.DebugTree()` для вывода логов в logcat.

**Последствия:** 
- Логи через `Timber.*` не будут видны в `adb logcat`
- В logcat будут видны только прямые вызовы `Log.*` и системные логи

**Рекомендация:** Добавить в DEBUG билдах:

```kotlin
if (BuildConfig.DEBUG) {
    Timber.plant(Timber.DebugTree())
}
```

**Обходное решение:** Использовать файловые логи через:
```bash
adb shell "run-as com.kotopogoda.uploader cat /data/data/com.kotopogoda.uploader/files/logs/app.log"
```

---

### Проблема 2: Двойная инициализация appLogger

**Описание:** В `KotopogodaUploaderApp.onCreate()`:

```kotlin
// Строка 112 - безусловное включение
appLogger.setEnabled(true)

// Строка 168 - может отключить на основе настроек
appLogger.setEnabled(settings.appLogging)
```

**Последствия:**
- Если пользователь ранее отключил логирование в настройках, оно сначала включится на ~1 секунду, а затем отключится
- Короткое окно записи нежелательных логов

**Рекомендация:** Убрать строку 112 и читать начальное значение из настроек синхронно, либо использовать флаг для первой инициализации.

---

## 📋 Инструкции для тестирования на реальном устройстве

### Шаг 1: Сборка и установка APK

```bash
./gradlew :app:assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

### Шаг 2: Запуск приложения

```bash
adb shell am start -n com.kotopogoda.uploader/.MainActivity
```

### Шаг 3: Проверка логов в logcat

```bash
# Очистить буфер
adb logcat -c

# Запустить приложение и выполнить действия
# ...

# Собрать логи
adb logcat -d > logcat.txt

# Фильтр по тегам приложения
adb logcat -d | grep -E "KotopogodaApp|WorkManager|NativeEnhance|Enhance/Probe|app"
```

⚠️ **Ожидание:** Логи через Timber, вероятно, не будут видны в logcat (см. Проблема 1).

### Шаг 4: Проверка файловых логов

```bash
# Список файлов логов
adb shell "run-as com.kotopogoda.uploader ls -lh /data/data/com.kotopogoda.uploader/files/logs/"

# Просмотр основного лог-файла
adb shell "run-as com.kotopogoda.uploader cat /data/data/com.kotopogoda.uploader/files/logs/app.log"

# Сохранение логов локально
adb shell "run-as com.kotopogoda.uploader cat /data/data/com.kotopogoda.uploader/files/logs/app.log" > app_logs.txt

# Если есть файлы enhance логов
adb shell "run-as com.kotopogoda.uploader ls /data/data/com.kotopogoda.uploader/files/logs/" | grep enhance
```

### Шаг 5: Сценарии тестирования

1. **Холодный старт:**
   - Очистить данные приложения
   - Запустить приложение
   - Проверить лог события `APP/START` / `application_create`

2. **Онбординг:**
   - Пройти через экраны онбординга
   - Проверить логи навигации и инициализации

3. **Привязка устройства:**
   - Отсканировать QR код
   - Проверить логи сетевых запросов (если httpLogging включён)

4. **Загрузка фото:**
   - Выбрать фото
   - Проверить логи работы с хранилищем и очередью загрузки

5. **Ошибочный сценарий:**
   - Отключить сеть
   - Попытаться загрузить фото
   - Проверить логи ошибок с полными стеками

6. **Изменение настроек:**
   - Зайти в настройки
   - Изменить baseUrl, отключить/включить логирование
   - Проверить лог события `CFG/STATE` / `settings`

7. **Фоновая загрузка:**
   - Поставить файл в очередь
   - Свернуть приложение
   - Проверить логи WorkManager и фоновой работы

### Шаг 6: Анализ логов

**Что проверить:**

- ✅ Наличие временных меток в каждой строке
- ✅ Уровни логирования (I, W, E, D)
- ✅ Теги модулей (app, WorkManager, NativeEnhance, и т.д.)
- ✅ Структурированные сообщения `UploadLog` с категориями и деталями
- ✅ Полные стеки при исключениях (не только сообщение)
- ✅ Читаемость и информативность для диагностики

**Примеры ожидаемых записей:**

```
[2024-01-15 10:23:45.123][I] WorkManager: category=WORK/Factory action=app_init
[2024-01-15 10:23:45.234][I] app: category=APP/START action=application_create
[2024-01-15 10:23:45.345][I] app: category=CFG/STATE action=settings base_url=https://... app_logging=true http_logging=true
[2024-01-15 10:23:45.456][I] app: category=PERM/STATE action=notifications granted=true
```

---

## 📊 Ожидаемые результаты

| Критерий | Статус | Комментарий |
|----------|--------|-------------|
| Логирование активно по-умолчанию | ✅ | `appLogging = true`, `httpLogging = true` |
| В logcat видны логи приложения | ⚠️ | Только прямые `Log.*` вызовы, не `Timber.*` |
| В файлах видны логи приложения | ✅ | `/data/data/.../files/logs/app.log` |
| Логи содержат полезную информацию | ✅ | Категории, действия, детали |
| Полные стеки исключений | ✅ | `Log.getStackTraceString(t)` |
| Логи структурированы | ✅ | Временные метки, уровни, теги |
| Ротация логов работает | ✅ | 5 файлов по 1MB |
| Экспорт логов доступен | ✅ | `LogManager.writeLogsArchive()` |

---

## 🔧 Рекомендуемые улучшения

### 1. Добавить DebugTree для DEBUG билдов

**Файл:** `app/src/main/java/com/kotopogoda/uploader/KotopogodaUploaderApp.kt`

**Изменение:**

```kotlin
override fun onCreate() {
    super.onCreate()
    
    // Добавить перед appLogger.setEnabled(true)
    if (BuildConfig.DEBUG) {
        Timber.plant(Timber.DebugTree())
    }
    
    appLogger.setEnabled(true)
    // ... остальной код
}
```

**Польза:** Логи через Timber будут видны в `adb logcat` для удобства разработки.

---

### 2. Исправить двойную инициализацию appLogger

**Файл:** `app/src/main/java/com/kotopogoda/uploader/KotopogodaUploaderApp.kt`

**Вариант А - Убрать безусловную инициализацию:**

```kotlin
override fun onCreate() {
    super.onCreate()
    
    // Убрать: appLogger.setEnabled(true)
    
    // ... остальной код
    
    scope.launch {
        settingsRepository.flow.collect { settings ->
            // Первая эмиссия установит правильное значение
            appLogger.setEnabled(settings.appLogging)
            // ...
        }
    }
}
```

**Вариант Б - Синхронное чтение настроек:**

```kotlin
override fun onCreate() {
    super.onCreate()
    
    val initialLogging = runBlocking {
        settingsRepository.flow.first().appLogging
    }
    appLogger.setEnabled(initialLogging)
    
    // ... остальной код
}
```

**Польза:** Исключение нежелательного короткого включения логирования, когда пользователь его отключил.

---

## 📎 Артефакты для сохранения

После тестирования на реальном устройстве сохранить:

1. ✅ `logcat.txt` - Полный logcat с запуска до закрытия приложения
2. ✅ `app_logs.txt` - Основной файл логов приложения
3. ✅ `enhance_logs.txt` - Логи Enhance операций (если есть)
4. ✅ Скриншоты экранов приложения во время тестирования
5. ✅ `diagnostics.json` - Экспортированная диагностика (если доступна через UI)
6. ✅ Этот отчёт с заполненными результатами тестирования

---

## 📝 Выводы

### Текущее состояние: ✅ ХОРОШЕЕ

- Логирование включено по-умолчанию
- Файловые логи структурированы и содержат полезную информацию
- Есть API для экспорта логов
- Логируются все критические события

### Требуется внимание: ⚠️

1. **Добавить DebugTree** для удобства разработки и видимости в logcat
2. **Исправить двойную инициализацию** appLogger для корректной работы с настройками

### Готовность к продакшену: ✅

Приложение готово к сборке и тестированию на реальном устройстве. Файловое логирование полностью функционально и включено по-умолчанию.

---

**Дата создания отчёта:** 2024
**Автор:** AI Code Review Agent
**Статус:** Проверка кода завершена ✅
